export default function reducer(store, action) {
    switch (action.type) {
        case "addItemsToCart": {
            return{};
        }
        case "xxx": {
            return{};
        }
        default: {
            return{};
        }
    }
}